# Red Team helpers (safe, consent-gated)
import shutil
import asyncio
import logging
import os
from typing import Dict, Optional

LOG = logging.getLogger("vulnez.redteam")
# ... (content same as prior redteam.py)
